<policies>
    <inbound>
        <base />

        <validate-client-certificate validate-revocation="false" validate-trust="false" 
                                     validate-not-before="true" validate-not-after="true" 
                                     ignore-error="false">
            <identities>
                <identity subject="CN=Client 01" issuer-subject="CN=APIM Sample DEV Intermediate CA" />
                <identity subject="CN=Client 02" issuer-subject="CN=APIM Sample DEV Intermediate CA" />
                <identity subject="CN=Expired Client" issuer-subject="CN=APIM Sample DEV Intermediate CA" />
            </identities>
        </validate-client-certificate>

        <!-- Returns a 200 OK with the client certificate details in the body -->
        <return-response>
            <set-status code="200" />
            <set-body>@(context.Request.Certificate.ToString())</set-body>
        </return-response>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>